//
//  Underline+Ext.swift
//  HabitBuilder
//
//  Created by Rustem Manafov on 04.12.22.
//

import UIKit

// MARK: Underline Label

/*
 
let underlineAttribute = [NSAttributedString.Key.underlineStyle: NSUnderlineStyle.thick.rawValue]
let underlineAttributedString = NSAttributedString(string: "write something", attributes: underlineAttribute)
label.attributedText = underlineAttributedString

 */

// MARK: Underline Button

/*
 
        fileprivate func underlineButton() {
            let attributeString = NSMutableAttributedString(
                string: "write something",
                attributes: yourAttributes)
            label.setAttributedTitle(attributeString, for: .normal)
        }

     //Underline Button second code
        let yourAttributes: [NSAttributedString.Key: Any] = [
            .font: UIFont.systemFont(ofSize: 14),
            .foregroundColor: UIColor(red: 0.34, green: 0.20, blue: 0.33, alpha: 1.00),
            .underlineStyle: NSUnderlineStyle.single.rawValue ]
 
 */
